# Force clean installation
cache-min=0
prefer-offline=false
fetch-retries=3
fetch-retry-mintimeout=5000
fetch-retry-maxtimeout=60000

# Ensure we get the latest compatible versions
save-exact=false
save-prefix=^
