{
  "name": "electrical-management-system",
  "version": "1.0.0",
  "description": "Electrical Management System with Backend and Frontend",
  "private": true,
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },

  "scripts": {
    "install": "cd Backend && npm ci --only=production",
    "start": "cd Backend && npm start",
    "build": "cd Backend && npm ci --only=production",
    "postinstall": "echo 'Forcing npm usage'"
  },
  "packageManager": "npm@8.0.0",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0",
    "yarn": ">=0.0.0"
  }
  "workspaces": [
    "Backend"
  ]
}